# 投資学習アプリ システムアーキテクチャ設計書

## ドキュメント情報

- **プロジェクト名**: 投資学習アプリ
- **作成日**: 2026 年 1 月 1 日
- **バージョン**: 1.0
- **対象リリース**: v1.0

---

## 1. システム全体概要

### 1.1 アーキテクチャ概要図

```
┌─────────────────────────────────────────────────────────────────┐
│                         ユーザー                                  │
│                           (iOS)                                  │
└────────────────────┬────────────────────────────────────────────┘
                     │
                     │ HTTP/HTTPS
                     ↓
┌─────────────────────────────────────────────────────────────────┐
│                  React Native アプリ                              │
│                       (Expo)                                     │
│  ┌──────────────┐         ┌──────────────┐                      │
│  │ニュースタブ  │         │  用語タブ    │                      │
│  └──────────────┘         └──────────────┘                      │
└────────────────────┬────────────────────────────────────────────┘
                     │
                     │ Firebase SDK
                     ↓
┌─────────────────────────────────────────────────────────────────┐
│                   Firebase Firestore                             │
│  ┌──────────────┐  ┌──────────────┐  ┌──────────────┐          │
│  │ニュースコレク│  │用語コレクショ│  │履歴コレクショ│          │
│  │ション        │  │ン            │  │ン            │          │
│  └──────────────┘  └──────────────┘  └──────────────┘          │
└─────────────────────────────────────────────────────────────────┘
                     ↑
                     │ HTTP (書き込み)
                     │
┌─────────────────────────────────────────────────────────────────┐
│              Vercel Serverless Functions                         │
│                                                                   │
│  ┌────────────────────────────────────────────────────────────┐ │
│  │          /api/daily-update (Cron Job: 毎日8:00)            │ │
│  │                                                              │ │
│  │  1. ニュース取得                                             │ │
│  │  2. AI要約処理                                               │ │
│  │  3. 用語生成処理                                             │ │
│  │  4. Firestoreへ保存                                          │ │
│  └────────────────────────────────────────────────────────────┘ │
└────────┬────────────────────────────────┬───────────────────────┘
         │                                │
         │ HTTPS                          │ HTTPS
         ↓                                ↓
┌─────────────────────┐        ┌─────────────────────────────────┐
│   NewsAPI           │        │   Anthropic Claude API          │
│   (世界ニュース)    │        │   (要約・用語生成)              │
└─────────────────────┘        └─────────────────────────────────┘
         │
         │ HTTPS
         ↓
┌─────────────────────┐
│ Google News RSS     │
│ (日本ニュース)      │
└─────────────────────┘
```

### 1.2 技術スタック一覧

| レイヤー           | 技術               | バージョン | 用途                              |
| ------------------ | ------------------ | ---------- | --------------------------------- |
| **フロントエンド** | React Native       | 0.73.x     | モバイルアプリ UI                 |
|                    | Expo               | SDK 50.x   | React Native 開発フレームワーク   |
|                    | TypeScript         | 5.x        | 型安全な開発                      |
|                    | React Navigation   | 6.x        | タブナビゲーション                |
| **バックエンド**   | Vercel             | -          | Serverless Functions ホスティング |
|                    | Node.js            | 18.x       | サーバーサイドランタイム          |
|                    | TypeScript         | 5.x        | 型安全な開発                      |
| **データベース**   | Firebase Firestore | -          | NoSQL データベース                |
| **外部 API**       | NewsAPI            | v2         | 世界ニュース取得                  |
|                    | Google News RSS    | -          | 日本ニュース取得                  |
|                    | Claude API         | 3.5 Sonnet | AI 要約・用語生成                 |
| **定期実行**       | Vercel Cron Jobs   | -          | 日次バッチ処理                    |
| **開発ツール**     | Git/GitHub         | -          | バージョン管理                    |
|                    | ESLint/Prettier    | -          | コード品質管理                    |

---

## 2. フロントエンド設計

### 2.1 アプリケーション構成

```
investment-learning-app/
├── app/                          # Expoルーター（画面）
│   ├── (tabs)/                   # タブナビゲーション
│   │   ├── _layout.tsx           # タブレイアウト
│   │   ├── news.tsx              # ニュースタブ
│   │   └── terms.tsx             # 用語タブ
│   └── _layout.tsx               # ルートレイアウト
├── components/                   # 再利用可能コンポーネント
│   ├── NewsCard.tsx              # ニュース表示カード
│   ├── TermCard.tsx              # 用語表示カード
│   └── LoadingSpinner.tsx        # ローディング表示
├── services/                     # ビジネスロジック
│   ├── firebase.ts               # Firebase設定・初期化
│   └── dataService.ts            # データ取得ロジック
├── types/                        # TypeScript型定義
│   ├── news.ts
│   └── term.ts
├── constants/                    # 定数定義
│   └── config.ts
├── app.json                      # Expo設定
├── package.json
└── tsconfig.json
```

### 2.2 画面設計

#### 2.2.1 ニュースタブ (news.tsx)

**画面構成**:

```
┌─────────────────────────────────────┐
│  📰 ニュース                         │
├─────────────────────────────────────┤
│                                     │
│  🌍 世界のニュース                   │
│  ┌─────────────────────────────┐   │
│  │ [更新日時: 2026-01-01 08:00] │   │
│  │                              │   │
│  │ 要約内容（約2000文字）        │   │
│  │ ...                          │   │
│  │ ...                          │   │
│  └─────────────────────────────┘   │
│                                     │
│  🇯🇵 日本のニュース                  │
│  ┌─────────────────────────────┐   │
│  │ [更新日時: 2026-01-01 08:00] │   │
│  │                              │   │
│  │ 要約内容（約2000文字）        │   │
│  │ ...                          │   │
│  │ ...                          │   │
│  └─────────────────────────────┘   │
│                                     │
└─────────────────────────────────────┘
```

**主要コンポーネント**:

- `ScrollView`: スクロール可能なコンテナ
- `NewsCard`: ニュース表示カード（世界・日本で再利用）
- `RefreshControl`: 引っ張って更新

#### 2.2.2 用語タブ (terms.tsx)

**画面構成**:

```
┌─────────────────────────────────────┐
│  📚 投資用語                         │
├─────────────────────────────────────┤
│                                     │
│  用語 1/3                           │
│  ┌─────────────────────────────┐   │
│  │ 📖 ROE（自己資本利益率）     │   │
│  │                              │   │
│  │ 解説：                       │   │
│  │ 企業が株主から預かった...    │   │
│  │ （約500文字）                │   │
│  └─────────────────────────────┘   │
│                                     │
│  用語 2/3                           │
│  ┌─────────────────────────────┐   │
│  │ 📖 PER（株価収益率）         │   │
│  │                              │   │
│  │ 解説：...                    │   │
│  └─────────────────────────────┘   │
│                                     │
│  用語 3/3                           │
│  ┌─────────────────────────────┐   │
│  │ 📖 配当利回り                │   │
│  │                              │   │
│  │ 解説：...                    │   │
│  └─────────────────────────────┘   │
│                                     │
└─────────────────────────────────────┘
```

**主要コンポーネント**:

- `ScrollView`: スクロール可能なコンテナ
- `TermCard`: 用語表示カード（3 つ表示）
- `RefreshControl`: 引っ張って更新

### 2.3 データフロー

```
[アプリ起動]
    ↓
[Firebase初期化]
    ↓
[Firestoreから今日のデータ取得]
    ↓
┌─ 成功 → [データを状態に保存] → [画面表示]
│
└─ 失敗 → [エラー表示] → [リトライボタン表示]
```

### 2.4 状態管理

**React Hooks ベースの状態管理**:

```typescript
// news.tsx
const [worldNews, setWorldNews] = useState<NewsData | null>(null);
const [japanNews, setJapanNews] = useState<NewsData | null>(null);
const [loading, setLoading] = useState(true);
const [error, setError] = useState<string | null>(null);

// terms.tsx
const [terms, setTerms] = useState<TermData[]>([]);
const [loading, setLoading] = useState(true);
const [error, setError] = useState<string | null>(null);
```

---

## 3. バックエンド設計

### 3.1 Vercel Serverless Functions 構成

```
api/
├── daily-update.ts          # メインバッチ処理
├── lib/
│   ├── newsapi.ts           # NewsAPI連携
│   ├── rss.ts               # Google News RSS連携
│   ├── claude.ts            # Claude API連携
│   └── firebase-admin.ts    # Firebase Admin SDK
└── types/
    ├── news.ts
    └── term.ts
```

### 3.2 日次バッチ処理フロー

```
[Vercel Cron: 毎日8:00 JST]
    ↓
[/api/daily-update 実行開始]
    ↓
┌───────────────────────────────────────┐
│ 1. ニュース取得処理                    │
│    ├─ NewsAPIから世界ニュース5件取得   │
│    └─ Google RSSから日本ニュース5件取得│
└───────────────────────────────────────┘
    ↓
┌───────────────────────────────────────┐
│ 2. AI要約処理                          │
│    ├─ 世界ニュースをClaude APIで要約   │
│    └─ 日本ニュースをClaude APIで要約   │
└───────────────────────────────────────┘
    ↓
┌───────────────────────────────────────┐
│ 3. 投資用語生成処理                    │
│    ├─ Firestoreから過去30日の用語取得  │
│    ├─ Claude APIで重複しない用語3つ生成│
│    └─ 各用語の解説をClaude APIで生成   │
└───────────────────────────────────────┘
    ↓
┌───────────────────────────────────────┐
│ 4. Firestoreへ保存                     │
│    ├─ newsコレクションに保存           │
│    ├─ termsコレクションに保存          │
│    └─ term_historyコレクションに記録   │
└───────────────────────────────────────┘
    ↓
┌───────────────────────────────────────┐
│ 5. 古いデータの削除                    │
│    └─ 30日以前のニュースを削除         │
└───────────────────────────────────────┘
    ↓
[処理完了 / エラー時はログ記録]
```

### 3.3 API 処理の詳細

#### 3.3.1 ニュース取得処理

```typescript
async function fetchWorldNews(): Promise<Article[]> {
  const response = await fetch(
    `https://newsapi.org/v2/top-headlines?category=business&language=en&pageSize=5`,
    {
      headers: {
        'X-Api-Key': process.env.NEWSAPI_KEY,
      },
    }
  );
  const data = await response.json();
  return data.articles;
}

async function fetchJapanNews(): Promise<Article[]> {
  const rssUrl = 'https://news.google.com/rss/search?q=投資+経済&hl=ja&gl=JP&ceid=JP:ja';
  const response = await fetch(rssUrl);
  const xml = await response.text();
  const parsed = parseRSS(xml);
  return parsed.items.slice(0, 5);
}
```

#### 3.3.2 AI 要約処理

```typescript
async function summarizeNews(articles: Article[]): Promise<string> {
  const prompt = `
以下の投資・経済ニュース記事を読み、全体を約2000文字で要約してください。
重要なポイントを押さえ、投資初心者にも分かりやすい日本語で書いてください。

記事:
${articles.map((a, i) => `${i + 1}. ${a.title}\n${a.description}`).join('\n\n')}
`;

  const response = await anthropic.messages.create({
    model: 'claude-3-5-sonnet-20241022',
    max_tokens: 4000,
    messages: [{ role: 'user', content: prompt }],
  });

  return response.content[0].text;
}
```

#### 3.3.3 投資用語生成処理

```typescript
async function generateTerms(excludeTerms: string[]): Promise<Term[]> {
  const prompt = `
投資初心者が学ぶべき投資用語を3つ選んで、それぞれ約500文字で解説してください。
以下の用語は最近出題されたので除外してください：
${excludeTerms.join(', ')}

JSON形式で以下のように出力してください：
[
  {
    "term": "用語名",
    "description": "解説文"
  },
  ...
]
`;

  const response = await anthropic.messages.create({
    model: 'claude-3-5-sonnet-20241022',
    max_tokens: 3000,
    messages: [{ role: 'user', content: prompt }],
  });

  return JSON.parse(response.content[0].text);
}
```

### 3.4 Vercel Cron 設定

**vercel.json**:

```json
{
  "crons": [
    {
      "path": "/api/daily-update",
      "schedule": "0 23 * * *"
    }
  ]
}
```

※ Vercel は UTC で動作するため、JST 8:00 = UTC 23:00（前日）

---

## 4. データベース設計

### 4.1 Firebase Firestore コレクション構成

```
投資学習アプリ (Firestore Database)
├── news                      # ニュースコレクション
│   └── {date}                # ドキュメントID: YYYY-MM-DD
│       ├── date: string      # 日付
│       ├── worldNews: object # 世界ニュース
│       │   ├── summary: string
│       │   └── updatedAt: timestamp
│       └── japanNews: object # 日本ニュース
│           ├── summary: string
│           └── updatedAt: timestamp
│
├── terms                     # 投資用語コレクション
│   └── {date}                # ドキュメントID: YYYY-MM-DD
│       ├── date: string      # 日付
│       ├── terms: array      # 用語配列
│       │   └── [
│       │       {
│       │         term: string,
│       │         description: string
│       │       }
│       │     ]
│       └── updatedAt: timestamp
│
└── term_history              # 用語履歴（重複チェック用）
    └── {termId}              # ドキュメントID: 自動生成
        ├── term: string      # 用語名
        ├── date: string      # 配信日
        └── createdAt: timestamp
```

### 4.2 データモデル詳細

#### 4.2.1 News ドキュメント

```typescript
interface NewsDocument {
  date: string; // "2026-01-01"
  worldNews: {
    summary: string; // 約2000文字の要約
    updatedAt: Timestamp; // 更新日時
  };
  japanNews: {
    summary: string; // 約2000文字の要約
    updatedAt: Timestamp; // 更新日時
  };
}
```

**例**:

```json
{
  "date": "2026-01-01",
  "worldNews": {
    "summary": "本日の世界経済ニュースでは...",
    "updatedAt": "2026-01-01T08:00:00Z"
  },
  "japanNews": {
    "summary": "日本経済では...",
    "updatedAt": "2026-01-01T08:00:00Z"
  }
}
```

#### 4.2.2 Terms ドキュメント

```typescript
interface TermsDocument {
  date: string; // "2026-01-01"
  terms: Array<{
    term: string; // 用語名
    description: string; // 約500文字の解説
  }>;
  updatedAt: Timestamp; // 更新日時
}
```

**例**:

```json
{
  "date": "2026-01-01",
  "terms": [
    {
      "term": "ROE（自己資本利益率）",
      "description": "企業が株主から預かった資本を使って..."
    },
    {
      "term": "PER（株価収益率）",
      "description": "株価が1株あたりの利益の何倍か..."
    },
    {
      "term": "配当利回り",
      "description": "株価に対する年間配当金の割合..."
    }
  ],
  "updatedAt": "2026-01-01T08:00:00Z"
}
```

#### 4.2.3 Term History ドキュメント

```typescript
interface TermHistoryDocument {
  term: string; // 用語名
  date: string; // "2026-01-01"
  createdAt: Timestamp; // 作成日時
}
```

### 4.3 インデックス設計

| コレクション | フィールド | 方向 | 用途                       |
| ------------ | ---------- | ---- | -------------------------- |
| news         | date       | DESC | 最新ニュース取得           |
| terms        | date       | DESC | 最新用語取得               |
| term_history | date       | DESC | 過去 30 日の用語取得       |
| term_history | term       | ASC  | 用語名検索（重複チェック） |

### 4.4 データ保持ポリシー

| コレクション | 保持期間 | 削除タイミング               |
| ------------ | -------- | ---------------------------- |
| news         | 30 日    | 日次バッチで 31 日以前を削除 |
| terms        | 30 日    | 日次バッチで 31 日以前を削除 |
| term_history | 無期限   | 削除なし（重複チェック用）   |

---

## 5. 外部 API 連携設計

### 5.1 NewsAPI 連携

**エンドポイント**: `https://newsapi.org/v2/top-headlines`

**パラメータ**:

```typescript
{
  category: 'business',
  language: 'en',
  pageSize: 5,
  apiKey: process.env.NEWSAPI_KEY
}
```

**レート制限**:

- 無料プラン: 100 リクエスト/日
- 使用頻度: 1 回/日
- 余裕: 十分

**エラーハンドリング**:

- タイムアウト: 10 秒
- リトライ: 3 回（指数バックオフ）
- フォールバック: 前日のニュースを表示

### 5.2 Google News RSS 連携

**エンドポイント**: `https://news.google.com/rss/search`

**パラメータ**:

```
q=投資+経済
hl=ja
gl=JP
ceid=JP:ja
```

**レート制限**:

- 制限なし
- 使用頻度: 1 回/日

**エラーハンドリング**:

- タイムアウト: 10 秒
- リトライ: 3 回
- フォールバック: 前日のニュースを表示

### 5.3 Claude API 連携

**エンドポイント**: `https://api.anthropic.com/v1/messages`

**モデル**: `claude-3-5-sonnet-20241022`

**使用量**:
| 処理 | 頻度 | 入力トークン | 出力トークン |
|------|------|-------------|-------------|
| 世界ニュース要約 | 1 回/日 | 5,000 | 3,000 |
| 日本ニュース要約 | 1 回/日 | 5,000 | 3,000 |
| 用語生成 | 3 回/日 | 500×3 | 750×3 |
| **合計** | **5 回/日** | **11,500** | **8,250** |

**月間コスト試算**:

- 入力: 11,500 × 30 = 345,000 トークン → $1.04
- 出力: 8,250 × 30 = 247,500 トークン → $3.71
- **合計: $4.75/月（約 714 円）**

**エラーハンドリング**:

- タイムアウト: 30 秒
- リトライ: 3 回（レート制限考慮）
- フォールバック: エラーメッセージを Firestore に保存

---

## 6. セキュリティ設計

### 6.1 認証・認可

**現行（v1.0）**:

- ユーザー認証なし（個人利用）
- Firestore は読み取り専用（アプリ側）
- 書き込みはサーバーサイドのみ

**Firestore Security Rules**:

```javascript
rules_version = '2';
service cloud.firestore {
  match /databases/{database}/documents {
    // 全てのコレクションで読み取りのみ許可
    match /{document=**} {
      allow read: if true;
      allow write: if false;  // アプリからの書き込み禁止
    }
  }
}
```

### 6.2 API キー管理

**環境変数**:

```bash
# Vercel環境変数
NEWSAPI_KEY=xxx
ANTHROPIC_API_KEY=xxx
FIREBASE_ADMIN_CREDENTIALS={"type":"service_account",...}

# Expo環境変数（.env）
EXPO_PUBLIC_FIREBASE_API_KEY=xxx
EXPO_PUBLIC_FIREBASE_PROJECT_ID=xxx
EXPO_PUBLIC_FIREBASE_APP_ID=xxx
```

**保護策**:

- `.env`ファイルは`.gitignore`に追加
- Vercel ダッシュボードで環境変数設定
- Firebase Admin SDK はサーバーサイドのみで使用

### 6.3 通信セキュリティ

- 全ての外部 API 通信は HTTPS
- Firebase SDK は自動的に暗号化通信
- 証明書の検証を有効化

---

## 7. パフォーマンス設計

### 7.1 フロントエンド最適化

| 項目             | 手法                                 |
| ---------------- | ------------------------------------ |
| データキャッシュ | Firestore のオフライン永続化を有効化 |
| 画像最適化       | 使用しない（テキストのみ）           |
| 遅延読み込み     | 不要（データ量が少ない）             |
| バンドルサイズ   | Tree shaking で不要なコードを削除    |

### 7.2 バックエンド最適化

| 項目           | 手法                                    |
| -------------- | --------------------------------------- |
| 並列処理       | ニュース取得と AI 処理を並列化          |
| タイムアウト   | 各 API 呼び出しに適切なタイムアウト設定 |
| 冪等性         | 同じ日付のデータは上書き（重複防止）    |
| エラー時の継続 | 一部失敗しても処理を続行                |

### 7.3 データベース最適化

| 項目         | 手法                                             |
| ------------ | ------------------------------------------------ |
| クエリ最適化 | 日付で直接ドキュメントを取得（インデックス不要） |
| データサイズ | 1 ドキュメント < 10KB（十分小さい）              |
| 読み取り削減 | オフライン永続化でキャッシュ活用                 |

---

## 8. 監視・ログ設計

### 8.1 ログ出力

**フロントエンド**:

```typescript
// 開発環境のみ詳細ログ
if (__DEV__) {
  console.log('[News] Fetching data...');
}

// エラーは常に記録
console.error('[News] Failed to fetch:', error);
```

**バックエンド**:

```typescript
// Vercel Functionsの標準出力
console.log('[daily-update] Starting batch process');
console.log('[daily-update] News fetched:', articles.length);
console.error('[daily-update] Error:', error);
```

### 8.2 監視項目

| 項目             | 監視方法              | アラート条件   |
| ---------------- | --------------------- | -------------- |
| バッチ成功率     | Vercel ログ確認       | 2 日連続失敗   |
| API 呼び出し回数 | Vercel ダッシュボード | 予算超過の恐れ |
| アプリクラッシュ | Expo 開発ツール       | -              |
| Firestore コスト | Firebase コンソール   | 無料枠 90%超過 |

### 8.3 エラー通知

**v1.0**: 手動確認（Vercel ダッシュボード、Firebase コンソール）

**将来的な改善**:

- Slack/Email 通知
- エラー率の自動集計
- Sentry などのエラートラッキングツール導入

---

## 9. デプロイ戦略

### 9.1 デプロイフロー

```
[開発]
  ↓
[ローカルテスト]
  ↓
[Gitコミット]
  ↓
[GitHub push]
  ↓
[Vercel自動デプロイ] ← バックエンド
  ↓
[Expo Build] ← フロントエンド
  ↓
[iOS実機テスト]
  ↓
[本番リリース]
```

### 9.2 環境構成

| 環境        | 用途         | デプロイ方法  |
| ----------- | ------------ | ------------- |
| Development | 開発・テスト | ローカル実行  |
| Production  | 本番利用     | Vercel + Expo |

**※v1.0 ではステージング環境なし（個人利用のため）**

### 9.3 ロールバック戦略

- **バックエンド**: Vercel ダッシュボードから前のデプロイに戻す
- **フロントエンド**: Expo ビルドの再配布
- **データベース**: Firebase は手動でデータ復元（バックアップなし）

---

## 10. スケーラビリティ設計

### 10.1 現行設計の限界

| 項目             | 現在   | 限界値                         |
| ---------------- | ------ | ------------------------------ |
| 同時ユーザー数   | 1 名   | 100 名程度（Firestore 無料枠） |
| データ保持期間   | 30 日  | 365 日（ストレージコスト増）   |
| ニュースソース数 | 2 種類 | 5 種類程度（API 制限）         |

### 10.2 将来的な拡張ポイント

**複数ユーザー対応時**:

1. ユーザーコレクション追加
2. Firebase Authentication の導入
3. ユーザーごとの設定・履歴管理

**機能拡張時**:

1. カテゴリ別ニュース配信
2. プッシュ通知（Expo Notifications）
3. 学習進捗の可視化（グラフ表示）

**パフォーマンス改善時**:

1. CDN の活用（Cloudflare 等）
2. GraphQL 導入（データ取得の最適化）
3. インクリメンタルビルド（Expo EAS）

---

## 11. 技術的負債と改善計画

### 11.1 既知の技術的負債

| 項目               | 現状               | 理想                | 優先度 |
| ------------------ | ------------------ | ------------------- | ------ |
| テストコード       | なし               | ユニット/E2E テスト | 中     |
| エラーハンドリング | 基本的なみ         | 詳細なエラー分類    | 高     |
| ログ集約           | 各所に console.log | 統一ロガー          | 低     |
| 型定義             | 部分的             | 完全な型安全性      | 中     |

### 11.2 改善計画（優先順位順）

1. **エラーハンドリング強化**（v1.1）
   - リトライロジックの実装
   - ユーザーフレンドリーなエラーメッセージ

2. **テストコード追加**（v1.2）
   - Jest + React Native Testing Library
   - API 呼び出しのモック化

3. **型定義の完全化**（v1.3）
   - Zod などのスキーマバリデーション
   - API レスポンスの型検証

---

## 12. 運用設計

### 12.1 日常運用タスク

| タスク         | 頻度    | 内容                               |
| -------------- | ------- | ---------------------------------- |
| アプリ起動確認 | 毎日    | ニュース・用語が正しく表示されるか |
| バッチログ確認 | 週 1 回 | Vercel ログでエラーがないか        |
| コスト確認     | 月 1 回 | Claude API、Firebase 使用量        |

### 12.2 定期メンテナンス

| タスク              | 頻度         | 内容                           |
| ------------------- | ------------ | ------------------------------ |
| 依存パッケージ更新  | 月 1 回      | `npm update`でセキュリティ更新 |
| Firebase データ確認 | 月 1 回      | 不要データの削除               |
| バックアップ        | 必要に応じて | Firestore エクスポート         |

### 12.3 障害対応手順

1. **バッチ処理失敗時**
   - Vercel ログで原因特定
   - 外部 API 障害の場合は復旧待ち
   - コード不具合の場合は修正・再デプロイ

2. **アプリクラッシュ時**
   - Expo ログで原因特定
   - データ不整合の場合は Firestore 修正
   - コード不具合の場合は修正・再ビルド

3. **コスト超過時**
   - 使用量を確認し、原因特定
   - 必要に応じて API 呼び出し頻度を削減
   - Haiku モデルへの切り替え検討

---

## 13. 成功指標と KPI

### 13.1 技術的 KPI

| 指標             | 目標値       | 測定方法                   |
| ---------------- | ------------ | -------------------------- |
| アプリ起動成功率 | 99%          | Expo 開発ツール            |
| バッチ処理成功率 | 98%          | Vercel ログ                |
| 平均起動時間     | 3 秒以内     | 手動測定                   |
| 月間コスト       | 1,000 円以下 | 各サービスのダッシュボード |

### 13.2 ユーザー行動 KPI

| 指標                       | 目標値    | 測定方法 |
| -------------------------- | --------- | -------- |
| 日次アクティブ利用         | 80%以上   | 手動記録 |
| 1 セッションあたりの読了率 | 70%以上   | 主観評価 |
| 継続利用日数               | 30 日達成 | 手動記録 |

---

## 14. 添付資料

### 14.1 参考リンク

- **React Native**: https://reactnative.dev/
- **Expo**: https://expo.dev/
- **Firebase Firestore**: https://firebase.google.com/docs/firestore
- **Vercel**: https://vercel.com/docs
- **NewsAPI**: https://newsapi.org/docs
- **Claude API**: https://docs.anthropic.com/

### 14.2 関連ドキュメント

- 要求・要件定義書（本ドキュメントと同梱）
- API 設計書（別途作成予定）
- データモデル定義書（本ドキュメント§4 参照）

---

## 改訂履歴

| バージョン | 日付       | 改訂内容 | 改訂者      |
| ---------- | ---------- | -------- | ----------- |
| 1.0        | 2026-01-01 | 初版作成 | KoheiMatsuo |

---

**承認**:

- アーキテクト: KoheiMatsuo
- 作成日: 2026 年 1 月 1 日
